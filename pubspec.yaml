name: food_explorer
description: A mobile app to explore recipes using the Spoonacular API.

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
   - assets/images/placeholder.png